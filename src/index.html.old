<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ultrasonic Scanner Interface</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        max-width: 1200px;
        margin: 0 auto;
      }

      /* Basic control styles */
      .control-section {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-bottom: 30px;
      }

      .control-row {
        display: flex;
        align-items: center;
        gap: 15px;
        padding: 15px;
        border: 1px solid #ddd;
        border-radius: 8px;
        background-color: #f9f9f9;
      }

      .control-group {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .control-group label {
        font-weight: bold;
        min-width: 120px;
      }

      select {
        flex: 1;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 14px;
        min-width: 200px;
      }

      select:disabled {
        background-color: #f5f5f5;
        color: #999;
      }

      button {
        padding: 8px 16px;
        border: none;
        border-radius: 4px;
        font-size: 14px;
        cursor: pointer;
        min-width: 80px;
      }

      button:enabled {
        background-color: #007bff;
        color: white;
      }

      button:enabled:hover {
        background-color: #0056b3;
      }

      button:disabled {
        background-color: #6c757d;
        color: #fff;
        cursor: not-allowed;
        opacity: 0.6;
      }

      /* Control panel styles */
      .control-panel {
        margin-bottom: 30px;
        padding: 20px;
        border: 2px solid #ddd;
        border-radius: 8px;
        background-color: #fff;
        min-height: 150px;
      }

      .control-panel h2 {
        margin-top: 0;
        margin-bottom: 15px;
        color: #333;
      }

      .placeholder-text {
        color: #666;
        font-style: italic;
        text-align: center;
        margin-top: 50px;
      }

      /* Chart container */
      #chart-container {
        width: 100%;
        min-height: 600px;
      }

      /* ===== RONGBUK DEVICES STYLES ===== */

      .rongbuk-container {
        padding: 20px;
        background-color: #fff;
        border: 2px solid #ddd;
        border-radius: 8px;
        margin-bottom: 30px;
      }

      /* Header - matches existing control panel style */
      .rongbuk-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 15px;
      }

      .rongbuk-title {
        font-size: 18px;
        font-weight: bold;
        color: #333;
        margin: 0;
      }

      .rongbuk-refresh-btn {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 16px;
        font-size: 14px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        min-width: auto;
        transition: background-color 0.15s ease;
      }

      .rongbuk-refresh-btn:hover:not(:disabled) {
        background-color: #0056b3;
      }

      .rongbuk-refresh-btn:disabled {
        background-color: #6c757d;
        opacity: 0.6;
        cursor: not-allowed;
      }

      /* Refresh icon animation */
      .refresh-spinning {
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        from {
          transform: rotate(0deg);
        }
        to {
          transform: rotate(360deg);
        }
      }

      /* Table - matches existing style */
      .rongbuk-table-container {
        border: 1px solid #ddd;
        border-radius: 8px;
        overflow: hidden;
        background-color: #fff;
      }

      .rongbuk-table {
        width: 100%;
        border-collapse: collapse;
      }

      .rongbuk-th {
        padding: 15px;
        text-align: left;
        font-size: 14px;
        font-weight: bold;
        color: #333;
        background-color: #f9f9f9;
        border-bottom: 1px solid #ddd;
      }

      .rongbuk-th-right {
        text-align: right;
      }

      .rongbuk-row {
        border-bottom: 1px solid #ddd;
        transition: background-color 0.15s ease;
      }

      .rongbuk-row:hover {
        background-color: #f9f9f9;
      }

      .rongbuk-td {
        padding: 15px;
        vertical-align: middle;
        border-bottom: 1px solid #eee;
      }

      .rongbuk-td-actions {
        text-align: right;
      }

      /* Device name cell */
      .rongbuk-device-name {
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .rongbuk-name {
        font-weight: bold;
        color: #333;
      }

      /* Location cell */
      .rongbuk-location {
        font-size: 14px;
        color: #666;
      }

      /* Status badges - using existing color scheme */
      .rongbuk-status {
        display: inline-flex;
        align-items: center;
        padding: 4px 10px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: bold;
        border: 1px solid;
      }

      .rongbuk-status-connected {
        background-color: #d4edda;
        color: #155724;
        border-color: #c3e6cb;
      }

      .rongbuk-status-transitioning {
        background-color: #fff3cd;
        color: #856404;
        border-color: #ffeaa7;
      }

      .rongbuk-status-disconnected {
        background-color: #f8f9fa;
        color: #6c757d;
        border-color: #dee2e6;
      }

      /* Action buttons - matches existing button style */
      .rongbuk-actions {
        display: flex;
        justify-content: flex-end;
        gap: 10px;
      }

      .rongbuk-btn {
        padding: 8px 16px;
        font-size: 14px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        min-width: 80px;
        transition: background-color 0.15s ease;
        color: white;
      }

      .rongbuk-btn:disabled {
        opacity: 0.6;
        cursor: not-allowed;
      }

      .rongbuk-btn-connect {
        background-color: #28a745;
      }

      .rongbuk-btn-connect:hover:not(:disabled) {
        background-color: #218838;
      }

      .rongbuk-btn-connect:disabled {
        background-color: #6c757d;
      }

      .rongbuk-btn-disconnect {
        background-color: #dc3545;
      }

      .rongbuk-btn-disconnect:hover:not(:disabled) {
        background-color: #c82333;
      }

      .rongbuk-btn-disconnect:disabled {
        background-color: #6c757d;
      }

      /* Empty state */
      .rongbuk-empty {
        padding: 50px 15px;
        text-align: center;
        color: #666;
        font-style: italic;
      }

      /* Device count */
      .rongbuk-count {
        margin-top: 10px;
        font-size: 14px;
        color: #666;
        text-align: right;
      }
    </style>
  </head>

  <body>
    <div id="root"></div>

    <script type="module">
      // This will be replaced by your React build process
      // For now, just a placeholder
      document.getElementById('root').innerHTML = `
            <div>
                <h1>Ultrasonic Scanner Interface</h1>
                <p style="color: #666; font-style: italic;">Loading React application...</p>
            </div>
        `;
    </script>
  </body>
</html>
